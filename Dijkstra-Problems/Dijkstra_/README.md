Dijkstra — Shortest Path in a Weighted Graph
🔍 What is the problem?

The goal of this problem is to find the minimum cost (shortest distance) path from vertex 1 to vertex n in a weighted undirected graph.

In simple words —
👉 “We want to know the least total edge weight required to travel from vertex 1 to vertex n, and which path achieves that.”
